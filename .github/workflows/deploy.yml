name: Deploy Website to Linux Server

on:
  push:
    branches:
      - main # Yalnızca main branch'ine push yapıldığında tetiklenir

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Kodu Depodan Çek (Checkout)
        uses: actions/checkout@v4

      #- name: SSH Anahtarını Ayarla
      #  uses: webfactory/ssh-agent@v0.9.0
      #  with:
      #    ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      #- name: Sunucuyu Bilinen Ağlara Ekle (Known Hosts)
      #  run: ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts

      #- name: Dosyaları Rsync ile Sunucuya Aktar
      #  run: |
      #    rsync -avz --delete \
      #    --exclude='.git/' \
      #    --exclude='.github/' \
      #    ./ ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }}:${{ secrets.TARGET_DIR }}